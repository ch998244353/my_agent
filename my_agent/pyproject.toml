[project]
name = "my-agent"
version = "0.1.0"
description = "A from-zero teaching project for building an Agents SDK-style agent."
requires-python = ">=3.10"
dependencies = [
    "openai>=2.31.0",
]

[tool.black]
line-length = 88
